package com.devexpress.acw_mcw_lib

import android.view.View

//Implementation of native interface in managed world is only way to provide meta information such as methods, properties or events to native world
interface TestInterface {
    fun someMethod()
}